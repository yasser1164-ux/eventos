// ---- SUPABASE CONFIG ------------------------------------------------------
// Paste your project's values here (Supabase Dashboard → Project Settings →
// API), then commit. The anon key is designed to be public: row level
// security only allows read access, so it's safe in client-side code.
//
// While these are placeholders, the app runs on the bundled seed data in
// seed.js instead — everything still works, just without the database.
const SUPABASE_URL = "YOUR_SUPABASE_URL";           // e.g. https://abcdefgh.supabase.co
const SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY"; // the long "anon / public" key
